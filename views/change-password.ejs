<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <%- include('header') %>

    <label for="current-password">Current Password:</label>
    <input type="password" id="current-password" name="current-password" value="admin"><br><br>
    <label for="new-password">New Password:</label>
    <input type="password" id="new-password" name="new-password" value="admin"><br><br>
    <label for="confirm-password">Confirm Password:</label>
    <input type="password" id="confirm-password" name="confirm-password" value="admin"><br><br>
    <button id="change-password">Change Password</button>
    <% if (user && user.verified) { %>
    <br>
    <p>OR:</p>
    <button id="send-password-reset-link">Send Password Reset Link</button>
    <% } %>

    <%- include('footer') %>

    <script src="/js/change-password/changePassword.js"></script>
    <% if (user && user.verified) { %>
    <script src="/js/change-password/sendPasswordResetLink.js" email="<%= user.email %>"></script>
    <% } %>
</body>

</html>
