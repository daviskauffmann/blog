<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <%- include('header') %>

    <div id="post"></div>

    <% if (user && user.username == username) { %>
    <button id="edit">Edit</button>
    <button id="delete">Delete</button>
    <% } %>

    <%- include('footer') %>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/marked/0.8.2/marked.js"></script>
    <script>
        class Renderer extends marked.Renderer {
            image(href, title, text) {
                return super.image(`/<%= username %>/images/${href}`, title, text);
            }
        }

        const renderer = new Renderer();
        document.getElementById('post').innerHTML = marked(`<%= post.content %>`, { renderer });

        // <% if (user && user.username == username) { %>
        document.getElementById('delete').onclick = e => {
            fetch('/admin/posts/<%= post._id %>', {
                method: 'DELETE',
            }).then(response => {
                if (response.redirected) {
                    window.location.href = response.url;
                }
            });
        };
        // <% } %>
    </script>
</body>

</html>
