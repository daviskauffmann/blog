<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <%- include('header') %>

    <div id="post"></div>

    <% if (user) { %>
    <button id="comment">Comment</button>
    <% if (user.roles.includes('admin')) { %>
    <button id="edit">Edit</button>
    <button id="delete">Delete</button>
    <% } %>
    <% } %>

    <%- include('footer') %>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/marked/0.8.2/marked.js"></script>
    <script>
        document.getElementById('post').innerHTML = marked(`<%= post.content %>`);

        // <% if (user) { %>
        document.getElementById('comment').onclick = e => {
            console.log('TODO: comment');
        };
        // <% if (user.roles.includes('admin')) { %>
        document.getElementById('edit').onclick = e => {
            console.log('TODO: edit');
        };
        document.getElementById('delete').onclick = e => {
            fetch('/admin/posts/<%= post._id %>', {
                method: 'DELETE',
            }).then(response => {
                if (response.redirected) {
                    window.location.href = response.url;
                }
            });
        };
        // <% } %>
        // <% } %>
    </script>
</body>

</html>
