<nav>
    <a href="/">Home</a> |
    <% if (user) { %>
    <% if (user.verified && user.roles.includes('admin')) { %>
    <a href="/admin/new-post">New Post</a> |
    <a href="/admin/images">Images</a> |
    <a href="/admin/new-image">New Image</a> |
    <% } %>
    <a href="/change-password">Change Password</a> |
    <a href="javascript:logout();">Logout</a>
    <% } else { %>
    <a href="/register">Register</a> |
    <a id="login">Login</a>
    <% } %>
</nav>

<script>
    // <% if (user) { %>
    function logout() {
        fetch('/logout', {
            method: 'POST',
        }).then(response => {
            window.location.href = '/';
        });
    }
    //<% } else { %>
    document.getElementById('login').href = `/login?returnurl=${window.location.href}`;
    // <% } %>
</script>
